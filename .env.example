# Server
PORT=4501
NODE_ENV=development

# MongoDB Atlas
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/earthtogether?retryWrites=true&w=majority

# JWT
JWT_SECRET=your_jwt_secret_key_here

# CORS (comma-separated list of allowed origins)
CORS_ORIGIN=http://localhost:3000,http://localhost:4501
